# Dependencies
node_modules/
npm-debug.log*
yarn-error.log*

# Environment / secrets (never commit these — set them in the Railway dashboard)
.env
.env.*
!.env.example

# Build output
dist/
build/

# Captured leads (ephemeral, per-instance backup; the real record is Notion)
data/

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
.claude/settings.local.json
